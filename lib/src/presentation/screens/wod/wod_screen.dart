import 'package:flutter/widgets.dart';

class WodScreen extends StatelessWidget {
  const WodScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
