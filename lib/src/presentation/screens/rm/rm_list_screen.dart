import 'package:flutter/widgets.dart';

class RmListScreen extends StatelessWidget {
  const RmListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('RM');
  }
}
