import 'package:flutter/widgets.dart';

class WodItemWidget extends StatelessWidget {
  const WodItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
